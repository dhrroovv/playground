[project]
name = "playground"
version = "0.1.0"
description = "A Python learning playground"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.141.1",
]

[tool.uv]
package = false
